development:
  # adapter: async
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: exchange_rates_development

test:
  adapter: async

production:
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: exchange_rates_production
